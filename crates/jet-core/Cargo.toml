[package]
name = "jet-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
toml = "0.8"

[dev-dependencies]
serde_json = "1"
serial_test = "3"
tempfile = "3"
